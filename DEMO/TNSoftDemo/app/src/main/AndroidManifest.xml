<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zing.demo">

    <application
        android:name=".application.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActMain"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActUniqueId"
            android:configChanges="orientation|keyboard|screenSize|screenLayout"></activity>
        <activity
            android:name=".ActXml2Java"
            android:configChanges="orientation|keyboard|screenSize|screenLayout"></activity>
        <activity
            android:name=".ActXml2JavaNormal"
            android:configChanges="orientation|keyboard|screenSize|screenLayout"></activity>
        <activity
            android:name=".ActXml2JavaStyle"
            android:configChanges="orientation|keyboard|screenSize|screenLayout"></activity>
        <activity
            android:name=".ActSubsequenceEmoji"
            android:configChanges="orientation|keyboard|screenSize|screenLayout">
            >
        </activity>
    </application>

</manifest>
